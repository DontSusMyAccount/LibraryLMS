import type { Metadata } from "next";

import ReservationsPage from "@/app/features/reservations/reservations.page";

export const metadata: Metadata = {
  title: "คิวจอง",
};

export default ReservationsPage;
